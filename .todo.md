# TODO

- [ ] Add `_buildWallpaperImage` helper in `wallpaper_settings_screen.dart` to support both asset and file paths with placeholder fallback.
- [ ] Use the helper for both the current wallpaper preview and each wallpaper grid item.
- [ ] Verify/ensure `wallpaper_service.dart` seeds and persists WAL1–WAL3 assets on initialization.
- [ ] Clean up any resulting lints (unused imports, etc.).
